"use strict";

function capitalizeName(name: string) : string {
    return name.toUpperCase();
}

console.log(capitalizeName("udit"));

//console.log(capitalizeName("uk"));

function even_number(num : number) : boolean {
    
    return (num%2 == 0);
}

